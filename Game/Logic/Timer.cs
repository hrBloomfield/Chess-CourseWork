namespace Game.Logic;

public class Timer
{
    private const int WHITE_TEN_MIN_TIMER = 600;
    private const int BLACK_TEN_MIN_TIMER = 600;
    private const int TEN_MIN_TIMER = 600;

    void TimerCountdown()
    {
        for(int i = 0; i <= TEN_MIN_TIMER; i++)
        {
            
        }
    }
}